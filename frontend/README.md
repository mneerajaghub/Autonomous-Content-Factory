# Frontend

This folder will contain the React-based frontend for the project. It will include the input form for users to paste text or URLs and display the generated outputs (blog, social posts, email).